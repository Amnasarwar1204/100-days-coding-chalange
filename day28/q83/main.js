function stringLength(str) {
    var upperStr = str.toLowerCase();
    var lowerStr = str.toUpperCase();
    console.log("Lowercase: ".concat(lowerStr, " , Uppercase: ").concat(upperStr));
}
stringLength("Happy coding");
