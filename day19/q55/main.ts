// Starts with a list of numbers
let num = [1,2,3,4,5,6];

// Doubles each number
let doublenum = num.map((num) =>{return num *2})
// let doublenum = num.map((num) => num *2)

// Shows the new list of doubled numbers
console.log(doublenum);// Output: [2, 4, 6, 8, 10]
// This line takes each number, doubles it, and puts it in a new list.