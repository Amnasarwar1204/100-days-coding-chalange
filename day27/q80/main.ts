// Starting with our car object
let carProperties = {
    make:"Tesla",
    model:"Model S",
    year:2023
}

// Adding a new property 'color' and updating 'year'
carProperties.color="Red";// Adds a new property 'color'
carProperties.year=2021;// Updates the 'year' property



// Showing the updated car object
console.log(carProperties); // Outputs the car object with the new color and updated year
// Now our car is blue and its model year is updated to 2021.