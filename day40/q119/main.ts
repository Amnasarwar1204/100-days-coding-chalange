// Initializes a counter
let create = 0
// This while loop runs until count is 5
while (create < 5) {
    console.log("Hello, World!")// Logs "Hello, World!"
    create++; // Increments count by 1
    
}
// Repeats logging "Hello, World!" 5 times, once per loop iteration.
